# LNG Tank Pressure Management

 Python scripts for simulating LNG tank pressure management scenarios.

## Requirements

Install the Python packages used by the project:

```bash
pip install numpy matplotlib scipy pandas CoolProp
```

## Running the analysis scripts

Run the scripts in `scripts/analysis` from the project root.

